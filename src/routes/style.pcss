@tailwind base;
@tailwind components;
@tailwind utilities;

input,
input:focus,
button,
button:focus,
select,
select:focus,
textarea,
textarea:focus {
    outline: 0;
}

input,
textarea {
    background: transparent;
}